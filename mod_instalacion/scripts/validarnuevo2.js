function formulario(f) {
if (f.nombreinstalacion.value   == '') { alert ('Nombre de la instalacion esta vacio');  
f.nombreinstalacion.focus(); return false; } 

if (f.sistema.value  == '') { alert ('Nombre del sistema esta vacio'); 
f.sistema.focus(); return false; }

if (f.equipo.value  == '') { alert ('Nombre del fabricante esta vacio'); 
f.equipo.focus(); return false; }

if (f.marca.value  == '') { alert ('la marca esta vacio'); 
f.marca.focus(); return false; }

if (f.modelo.value  == '') { alert ('Numero de serie esta vacio'); 
f.modelo.focus(); return false; }

if (f.codigo.value  == '') { alert ('Nombre de Proveedor esta vacio'); 
f.codigo.focus(); return false; }

if (f.unidadpotencia.value  == '') { alert ('Unidad de potencia esta vacio'); 
f.unidadpotencia.focus(); return false; }

if (f.nombrefabricante.value  == '') { alert ('Nombre del fabricante esta vacio'); 
f.nombrefabricante.focus(); return false; }

if (f.versionsoftware.value  == '') { alert ('La version de software esta vacio'); 
f.versionsoftware.focus(); return false; }

if (f.nombreproveedor.value  == '') { alert ('El nombre del proveedor esta vacio'); 
f.nombreproveedor.focus(); return false; }

if (f.codigoproveedor.value  == '') { alert ('El codigo del proveedor esta vacio'); 
f.codigoproveedor.focus(); return false; }

if (f.recinto.value  == '') { alert ('Fecha de caducidad de la garantia esta vacio'); 
f.recinto.focus(); return false; }

